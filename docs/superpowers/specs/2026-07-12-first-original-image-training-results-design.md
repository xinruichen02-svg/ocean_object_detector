# 第一次原图训练结果上传设计

## 目标

将 `E:\duo_result_v1` 中的云端训练输出完整、可核验地保存到仓库 `xinruichen02-svg/ocean_object_detector` 的 `main` 分支，改动名称为“第一次原图训练”。

## 仓库布局

- 目标目录：`results/duo_result_v1/`
- 原有 32 个训练输出文件保持原始文件名和相对结构。
- `weights/` 中的 10 个 `.pt` 权重全部保留。
- 新增 `results/duo_result_v1/README.md`，说明训练配置、关键指标、文件用途和已发现的重复预览图。
- 不修改仓库中现有的数据集、代码、文档模板或其他结果目录。

## README 内容

- 训练配置：YOLO11n、80 epochs、batch 16、640 px、seed 42、确定性训练、无数据增强。
- 最佳综合结果：epoch 23，mAP50 为 0.74291，mAP50-95 为 0.52387。
- 最终结果：epoch 80，precision 为 0.82312，recall 为 0.64644，mAP50 为 0.7148，mAP50-95 为 0.51138。
- 总训练时间：14,588.2 秒，约 4.052 小时。
- 权重说明：`best.pt`、`last.pt` 和每 10 轮保存的 checkpoint。
- 重复说明：`train_batch0/1/2.jpg` 分别与 `train_batch24850/24851/24852.jpg` 内容完全相同；为保留原始云端输出，全部上传。

## 验证

- 源目录应包含 32 个文件，总大小 186,749,783 字节。
- 所有单文件均小于 GitHub 100 MB 限制。
- 复制后逐文件核对相对路径、大小和 SHA-256。
- 提交前确认只包含本设计文档、结果目录和结果 README。
- 推送后确认远端 `main` 与本地提交 SHA 一致。

## 提交

- 分支：`main`
- 提交信息：`第一次原图训练`
